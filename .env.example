# Database (PostgreSQL - Neon)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# App URL (set this to your deployed domain for OAuth callbacks)
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
